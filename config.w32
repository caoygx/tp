// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("tp", "for tp support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("tp", "enable tp support", "no");

if (PHP_TP != "no") {
	EXTENSION("tp", "tp.c");
}

